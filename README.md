# File Renamer App

This app changes the names of the files in the folder named **`files`**.

The application is made in **Python** using the built-in **`os`** library.

At the moment, this is my **first official project** using this programming language.
